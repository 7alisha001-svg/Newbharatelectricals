RewriteEngine On
RewriteBase /

# Preserve API routes and asset requests
RewriteRule ^api/ - [L]

# SPA fallback for client-side routes
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
